
### Task #1:
    
    Default permission for a newly created file and directory:
    1. Directory: drwxr-xr-x
    2. File: -rw-r--r--

### Task #2:
 
    Create a user using Linux 
        Created a user as super user using the command useradd
 
### Task #3:
 
    Delete the above user
        deleted the use use userdel

### Task #4:
    Create another user
    change the password 
    and switch to 2nd user

        Created user with useradd command and changed password use passwd command
        switched user with su command 

### Task #5:
    Find your system's current time 
        used date to display time along with date
        used date +%T for time alone

### Task #6:
    What is the chown command used for ? 
        It is used to change the ownership and user group of a file or folder

### Task #7:
    What is grep used for? 
        it is used for patter matching and search through a file or codebase.
        we can use regex for pattern matching.

### Task #8:
    How to use redirecting in linux?
        we use > to redirect the output of a command into a file or output stream.
        (> overwrite the file , >> appeds to a file)

### Task #9:
    Try to open any file using vi command.
        vi is a simple terminal based text editor.
    
### Task #10:
    Try to use hostname command
        It displays the system name(hostname)

### Task #11:
    Try to use name -a to display detailed system information including kernel versions
        It displays the name of the system kernel versions local time and other system details

### Task #12:
    Try to use sudo command(write any command here)
        I used sudo su to get super user shell

### Task #13:
    What do you understand by HTTP and HTTPS:
        HTTP is a protocol to transfer and communicate text or textual data throgh the internet with two or more machines.
        HTTPS is a encrypted version of the HTTP, where the content is encrypted.

### Task #14:
    What are methods in HTTP protocols:
        1. GET
        2. POST
        3. PUT
        4. DELETE
        5. QUERY

### Task #15:
    GIve few status codes in HTTP protocols:
        2xx: ie starting in the range or 200 and above are success codes
        4xx: are error codes

### Task #16:
    What do you understand by Headers in HTTP
        these contain the details about the connection and other important meta data.

